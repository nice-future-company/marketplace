# OS files
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*.swo

# Local plugin settings
*.local.md

# Node (if scripts use npm later)
node_modules/

# Environment
.env
.env.local

# PII — 멘티 vault (로컬 전용, 절대 git 추적 금지)
# 기본 vault 위치는 ~/.resume-vaults/ (repo 외부).
# mentee-vault/ 는 repo 내부 경로를 vault_root로 지정한 경우를 대비한 안전망.
mentee-vault/
*-vaults/
*.resume-session
.resume-config.json

# 유료 데이터팩 (미출시, 로컬 전용)
data/mentoring/topics/
